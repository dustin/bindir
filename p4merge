#!/bin/sh
#
# use vimdiff to get easy merges in p4 resolve

# The file revision synced to the client workspace before it was opened for
# edit. 
base=$1
# The head revision of the file in the depot.
theirs=$2
# The revision of the file in the client workspace
yours=$3
# A file version generated by Perforce from yours, theirs, and base. The user
# can edit this revision during the resolve process if the file is a text file.
merge=$4

# Put my file in the merge results to get us ready for editing
cp $yours $merge
# Start a vimdiff editing my file as compared to the one from integration
$VISUAL -d $merge $theirs
